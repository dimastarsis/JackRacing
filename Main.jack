class Main
{
   function void main()
   {
      var RaceGame game;

      while (true)
      {
         do Screen.clearScreen();
         do Interface.drawWelcomeWindow();
         do Sys.wait(100);
         let game = RaceGame.new();
         do game.run();
         do game.dispose();
      }

      return; 
   }
}