class Main
{
   function void main()
   {
      var RaceGame game;
      
      while (true)
      {
         do Interface.drawWelcomeWindow();
         let game = RaceGame.new();
         do game.run();
         do Interface.drawFinalWindow(game.GetScore());
         do game.dispose();
      }

      return; 
   }
}