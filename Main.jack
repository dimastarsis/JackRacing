class Main
{
    function void main()
    {
        var RaceGame game;
        let game = RaceGame.new();
        // do game.run();
        do game.draw(0);
        do game.draw(480);
        return;
    }
}